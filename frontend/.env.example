# URL base da API do Gestor de Inadimplência (backend Node/Express).
# Usada no client (por isso o prefixo NEXT_PUBLIC_) para montar as chamadas
# de login, listagem de associados, detalhe e atualização de negociação.
# Não incluir barra "/" no final.
NEXT_PUBLIC_API_URL=https://api.exemplo.com
